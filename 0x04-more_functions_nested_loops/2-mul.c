#include "main.h"

/**
 * mul - check if a charcter is a digit
 * @x: The number to be checked
 * @y: second number to mul x
 * Return: the result multiple x param and y param
 */

int mul(int x, int y)

{

	return (x * y);

}
